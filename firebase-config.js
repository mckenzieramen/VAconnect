/*
 * VA CONNECT Firebase Web App configuration.
 *
 * This file is intentionally separate so the public website can use Firebase
 * Authentication + Firestore without exposing any server credentials.
 * Replace ONLY these public Firebase Web App values with the config from
 * Firebase Console > Project settings > Your apps > Web app.
 */
window.VA_FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: ""
};
